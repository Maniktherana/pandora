{
  "name": "@pandora/daemon",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts src/pty-worker.ts --target node --packages external --outdir dist",
    "dev": "bun run build && node dist/index.js",
    "start": "node dist/index.js",
    "smoke:pty": "bun build src/smoke-pty.ts --target node --packages external --outfile dist/smoke-pty.js && node dist/smoke-pty.js",
    "test": "bun test",
    "lint": "oxlint -c oxlint.json src/",
    "fmt": "oxfmt src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "node-pty": "^1.0.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/bun": "^1.3.11",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0"
  }
}
