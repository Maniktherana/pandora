{
  "name": "@pandora/daemon",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "smoke:pty": "bun run src/smoke-pty.ts",
    "test": "bun test",
    "lint": "oxlint -c oxlint.json src/",
    "fmt": "oxfmt src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pino": "^10.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0"
  }
}
